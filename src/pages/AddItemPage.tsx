import { PageLayout } from '@/components/layout/PageLayout';
import { PlaceholderCard } from '@/components/ui/PlaceholderCard';

export function AddItemPage() {
  return (
    <PageLayout title="Add item" description="Create a single vocabulary, kanji, or grammar entry.">
      <PlaceholderCard>Single-item form will be implemented here.</PlaceholderCard>
    </PageLayout>
  );
}
