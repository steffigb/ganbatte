import { PageLayout } from '@/components/layout/PageLayout';
import { PlaceholderCard } from '@/components/ui/PlaceholderCard';

export function BulkImportPage() {
  return (
    <PageLayout title="Bulk import" description="Import CSV or pasted text with preview and duplicate handling.">
      <PlaceholderCard>Bulk import flow will be implemented in the import feature.</PlaceholderCard>
    </PageLayout>
  );
}
